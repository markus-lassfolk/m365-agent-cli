/**
 * Commit SHA for this build. Run `npm run embed-sha` from a git checkout before release
 * so GlitchTip eligibility can match GitHub. Default `unknown` skips the git match unless relaxed.
 */
export const COMMIT_SHA = 'unknown';
